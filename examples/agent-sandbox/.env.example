# Your Kisenon project id (used for `keon` calls).
KISENON_PROJECT_ID=

# Your project's main-branch connection string. Used to apply setup.sql against main.
KISENON_URL=

# LLM provider keys. The CLI reads whichever matches --provider (defaults to anthropic).
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
